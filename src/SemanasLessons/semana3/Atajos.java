package SemanasLessons.semana3;

public class Atajos {
    public static void main(String[] args) {
        //psvm + tab: crear método main
        //sout + tab: System.out.println();
        //fori + tab: for(int i = 0; i < args.length; i++){}
        //iter + tab: for (String arg : args) {}
        //if + tab: if () {}
        //psfs + tab: public static final String
        //ctrl * alt * t: surround with
        //ctrl * alt * l: formatear código
        //ctrl * shift * A: buscar acción
        for (int i = 0; i < 10; i++) {
            System.out.println("Hola mundo");
        }
    }
}
